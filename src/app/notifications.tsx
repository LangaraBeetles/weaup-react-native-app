import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const NotificationsScreen = () => {
  return (
    <SafeAreaView>
      <Text>Notifications Page text</Text>
    </SafeAreaView>
  );
};

export default NotificationsScreen;
