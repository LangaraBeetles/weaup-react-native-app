import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const StreakScreen = () => {
  return (
    <SafeAreaView>
      <Text>Streak Page text</Text>
    </SafeAreaView>
  );
};

export default StreakScreen;
