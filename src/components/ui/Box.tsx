import { styled } from "@fast-styles/react";
import { View } from "react-native";

const Box = styled(View, {
  display: "flex",
  width: "100%",
  padding: 16,
  backgroundColor: "#fff",
  borderRadius: 16,
});

export default Box;
